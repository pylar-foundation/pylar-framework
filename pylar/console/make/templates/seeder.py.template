"""$class_name — generated by `pylar make:seeder`."""

from __future__ import annotations

from pylar.database import Seeder, transaction


class $class_name(Seeder):
    """Seed the database with initial data.

    Register this seeder in your AppServiceProvider::

        container.tag([$class_name], SEEDERS_TAG)

    Then run: ``pylar db:seed``
    """

    async def run(self) -> None:
        async with transaction():
            pass  # TODO: create your seed records
