"""$class_name job — generated by `pylar make:job`."""

from __future__ import annotations

from pylar.queue import Job, JobPayload


class ${class_name}Payload(JobPayload):
    """Typed payload for ``$class_name``. Add fields the job needs."""


class $class_name(Job[${class_name}Payload]):
    payload_type = ${class_name}Payload

    async def handle(self, payload: ${class_name}Payload) -> None:
        # TODO: implement the job's work here.
        pass
