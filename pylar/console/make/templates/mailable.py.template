"""$class_name mailable — generated by `pylar make:mailable`."""

from __future__ import annotations

from pylar.mail import Mailable, Message


class $class_name(Mailable):
    def __init__(self, recipient: str) -> None:
        self.recipient = recipient

    async def build(self) -> Message:
        return Message(
            to=(self.recipient,),
            subject="$class_name",
            text="TODO: write the body of $class_name",
        )
