"""$class_name notification — generated by `pylar make:notification`."""

from __future__ import annotations

from pylar.notifications import Notifiable, Notification


class $class_name(Notification):
    def via(self) -> tuple[str, ...]:
        return ("log",)

    def to_log(self, notifiable: Notifiable) -> str:
        return f"$class_name for {notifiable.routes_for('log')}"
