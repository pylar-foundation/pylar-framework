"""$class_name observer — generated by `pylar make:observer`."""

from __future__ import annotations

from typing import Any

from pylar.database import Observer


class $class_name(Observer[Any]):
    """TODO: replace ``Any`` with the concrete model class this observer watches."""

    async def creating(self, instance: Any) -> None:
        pass

    async def updating(self, instance: Any) -> None:
        pass

    async def deleting(self, instance: Any) -> None:
        pass
