"""$class_name factory — generated by `pylar make:factory`."""

from __future__ import annotations

from typing import Any

from pylar.testing import Factory


class $class_name(Factory[Any]):
    """TODO: replace ``Any`` with the concrete Model subclass."""

    @classmethod
    def model_class(cls) -> type:
        raise NotImplementedError("Set model_class to your Model subclass")

    def definition(self) -> dict[str, object]:
        return {}
